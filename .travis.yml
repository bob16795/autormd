language: python
python:
  - "3.6"
  - "3.6-dev"
# command to install dependencies
install:
  - sudo apt-get install -y python3-dev python3-pip
  - sudo apt-get install -y r-base texlive-full
  - R -e "packages.install('rmarkdown')"
  - pip3 install setuptools
  - python3 setup.py install
# command to run tests
script:
  - autormd setup
  - autormd add lol nope
  - autormd compile
